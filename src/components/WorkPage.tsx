import Work from './Work';
import Gallery from './Gallery';

// The Work page pairs the project/store-link cards with a visual gallery
// of real in-app screens — links to prove it ships, screenshots to show it.
export default function WorkPage() {
  return (
    <>
      <Work />
      <Gallery />
    </>
  );
}
